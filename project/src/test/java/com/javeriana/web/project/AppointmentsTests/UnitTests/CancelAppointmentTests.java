package com.javeriana.web.project.AppointmentsTests.UnitTests;

import com.javeriana.web.project.Appointments.Appointment.Application.Cancel.CancelAppointment;
import com.javeriana.web.project.Appointments.Appointment.Domain.Appointment;
import com.javeriana.web.project.Appointments.Appointment.Domain.Ports.AppointmentRepository;
import org.junit.jupiter.api.Test;

import static org.mockito.Mockito.*;

public class CancelAppointmentTests {
/*
    @Test
    void should_cancel_appointment(){
        AppointmentRepository repository = mock(AppointmentRepository.class);
        CancelAppointment cancelAppointment = new CancelAppointment(repository);
        Appointment canceledAppointment = mock(Appointment.class);
        cancelAppointment.execute("4a229224-759d-4234-8871-ef352ca516ce");
        verify(repository, atLeastOnce()).delete(canceledAppointment);
    }
 */
}
